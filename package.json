{
  "name": "youtube-channel-insight",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npm run dev --prefix frontend",
    "dev:backend": "composer -d backend run dev",
    "dev:frontend": "npm run dev --prefix frontend",
    "lint": "npm run lint:check --prefix frontend && composer -d backend run lint",
    "test": "npm run test:unit --prefix frontend -- --run && composer -d backend run test",
    "build": "npm run build --prefix frontend && composer -d backend run build"
  }
}
